{
  "name": "boldify",
  "displayName": "Boldify",
  "description": "Apply bold formatting to selected text based on file type",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Formatters"
  ],
  "publisher": "TinyTanic",
  "author": {
    "name": "Luca Aguzzoli"
  },
  "repository": {
    "url": "https://github.com/TinyTanic/boldify"
  },
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "boldify.boldify",
        "title": "Boldify Selected Text"
      }
    ],
    "keybindings": [
      {
        "command": "boldify.boldify",
        "key": "ctrl+b ctrl+b",
        "mac": "cmd+b cmd+b",
        "when": "editorTextFocus"
      }
    ],
    "configuration": {
      "title": "boldify",
      "properties": {
        "boldify.customFormats": {
          "type": "object",
          "default": {},
          "description": "Custom bold format templates for each language. Use {text} as placeholder for selected text.",
          "patternProperties": {
            ".*": {
              "type": "string",
              "description": "Bold format template"
            }
          },
          "additionalProperties": {
            "type": "string"
          },
          "markdownDescription": "Map language IDs to bold format templates. Use `{text}` as placeholder.\n\n| Filename | Bold |\n|----------|------|\n| markdown | `**{text}**` |\n| html | `<b>{text}</b>` |",
          "examples": [
            {
              "markdown": "**{text}**",
              "html": "<b>{text}</b>",
              "typescript": "<b>{text}</b>",
              "javascript": "<b>{text}</b>"
            }
          ]
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "package": "vsce package",
    "publish": "vsce publish",
    "vsce": "vsce"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.80.0",
    "@vscode/vsce": "^3.7.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  }
}